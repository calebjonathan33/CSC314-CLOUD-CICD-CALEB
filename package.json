{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "build": "webpack --config webpack.config.js"  // Ensure this command produces output in the expected directory
  },
  "dependencies": {
    // Your dependencies here
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0"
  }
}
